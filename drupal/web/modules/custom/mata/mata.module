<?php

/**
 * @file
 * Contains mata.module.
 */

use Drupal\Core\Routing\RouteMatchInterface;

/**
 * Implements hook_help().
 */
function mata_help($route_name, RouteMatchInterface $route_match) {
  switch ($route_name) {
    // Main module help for the mata module.
    case 'help.page.mata':
      $output = '';
      $output .= '<h3>' . t('About') . '</h3>';
      $output .= '<p>' . t('My Awesome Module') . '</p>';
      return $output;

    default:
  }
}

/**
 * Implements hook_theme().
 */
function mata_theme() {
  return [
    'mata' => [
      'render element' => 'children',
    ],
  ];
}
